# B0.5 — First-Run Trust + AI Access Closure

Status: **CLOSED**

Contract authority:

- `REBUILD-CONTRACT.md`
- `docs/vnext/00-FIRST-RUN-TRUST-AND-AI-ACCESS.md`
- `docs/vnext/01-AI-PROVIDER-ROUTING.md`
- `docs/vnext/02-BYOK-SECRET-HANDLING.md`
- `docs/vnext/03-GEMINI-MODEL-MATRIX.md`

## Implemented foundation

```text
TRUST_DISCLOSURE                  PASS
EXPLICIT_ACKNOWLEDGEMENT          PASS
AUTHENTICATED_SESSION_BOUNDARY    PASS
CONSENT_RECEIPT                   PASS
AI_ACCESS_MODE_SELECTION          PASS
NO_CLOUD_TRUSTED_CORE_PATH        PASS
PLATFORM_KEY_SERVER_ONLY_SCHEMA   PASS
BYOK_MEMORY_ONLY_STORE            PASS
BYOK_REMOTE_HTTP_REFUSAL_POLICY   PASS
LOOPBACK_HTTP_DEV_EXCEPTION       PASS
GATEWAY_PROVIDER_ABSTRACTION      PASS
CAPABILITY_MODEL_ROUTE_BASELINE   PASS
NO_CLOUD_SKIPS_GEMINI_PLAN        PASS
```

## Executable evidence

`src/domain/b0-5-contracts.test.ts` proves:

- disclosure semantics are versioned;
- stale disclosure receipts are invalid;
- raw BYOK cannot enter durable AI preference schema;
- BYOK store is non-enumerable and clearable;
- BYOK transport is limited to HTTPS or explicit loopback HTTP;
- capability routing follows the signed model matrix;
- no-cloud mode produces no Gemini attempt;
- cloud-enabled mode plans Gemini before Ollama.

`src/security/b0-5-secret-boundary.test.ts` proves source-level secret boundaries:

- platform Gemini secret is not a `NEXT_PUBLIC_*` variable;
- browser persistence APIs are absent from BYOK handling;
- current Supabase SSR auth primitives are used.

The UI consumes the BYOK secure-origin policy and disables the credential path on insecure remote HTTP.

## Closure receipt

The complete construction verification passed on the exact implementation head containing the B0.5 changes:

```text
head_sha    7c8e771897a5981e49484c23ea4c8ec0924f8432
workflow    CV Engine vNext Construction
run         33354071620
conclusion  success
```

The closure-ledger promotion commit changes status documentation only. Its own GitHub checks are part of the external closure receipt: if either required workflow is red, this `CLOSED` declaration is invalid and the node reverts to `CANDIDATE` by policy.

## Deferred dynamic provider evidence

The following acceptance criteria require a real provider invocation path that intentionally belongs to B6/B8 rather than the B0.5 foundation:

```text
SERVER_PROVIDER_LOG_SECRET_CANARY
OLLAMA_FALLBACK_NEVER_RECEIVES_GEMINI_SECRET
NORMALIZED_PROVIDER_AUTH_ERROR_NEVER_ECHOES_SECRET
PROVIDER_PROVENANCE_CONTAINS_NO_SECRET
```

These are inherited release blockers. Their deferral does not weaken the B0.5 foundation and does not permit B6/B8 to close without them.

## Closure declaration

```text
CONTRACT_SIGNED       PASS
IMPLEMENTED           PASS
WIRED                 PASS
EXECUTABLE_TESTS      PASS
CONSTRUCTION_CI       PASS
OPEN_B0_5_BLOCKERS    NONE
B0.5                  CLOSED
```

Reopen B0.5 only if new evidence invalidates one of these predicates.
